apiVersion: v1
kind: Service
metadata:
  name: myapp-svc
  labels:
    app: nginx
    environment: development
    type: webserver
    tier: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    environment: development