apiVersion: v1
kind: Service
metadata:
  name: load-balancer-svc
  labels:
    app: nginx
    environment: development
    type: webserver
    tier: backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    environment: development
    